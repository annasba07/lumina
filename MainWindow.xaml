<Window x:Class="SuperWhisperWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.modernwpf.com/2019"
        Title="SuperWhisper"
        Width="900" Height="700"
        MinWidth="600" MinHeight="500"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource AppBackgroundBrush}"
        ui:WindowHelper.UseModernWindowStyle="True">

    <Grid Margin="24,32,24,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Clean Header -->
        <Border Grid.Row="0" Style="{StaticResource SectionCard}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <!-- Clean Wordmark -->
                <StackPanel Grid.Column="0" Orientation="Vertical">
                    <TextBlock Text="SuperWhisper" FontSize="32" FontWeight="Light" 
                             Foreground="{StaticResource TextPrimaryBrush}" 
                             Margin="0,0,0,4"/>
                    <TextBlock Text="AI Speech Recognition" FontSize="15" FontWeight="Normal" 
                             Foreground="{StaticResource TextTertiaryBrush}"/>
                </StackPanel>
                
                <!-- Status Area -->
                <StackPanel Grid.Column="1" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,0,8">
                        <Ellipse x:Name="StatusIndicator" Width="8" Height="8" 
                               Fill="{StaticResource AccentBrush}" Margin="0,0,8,0"/>
                        <TextBlock x:Name="StatusText" Text="Ready" FontSize="14" FontWeight="Medium"
                                 Foreground="{StaticResource TextSecondaryBrush}"/>
                    </StackPanel>
                    <TextBlock x:Name="EngineStatusText" Text="Whisper.net" FontSize="12" 
                             Foreground="{StaticResource TextTertiaryBrush}" HorizontalAlignment="Right"/>
                </StackPanel>
            </Grid>
        </Border>
        
        <!-- Content Area -->
        <Border Grid.Row="1" Style="{StaticResource SectionCard}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <!-- Recording Controls -->
                <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,0,0,32">
                    <TextBlock Text="Press Ctrl+Space to record" FontSize="16" FontWeight="Medium"
                             Foreground="{StaticResource TextSecondaryBrush}" 
                             HorizontalAlignment="Center" Margin="0,0,0,12"/>
                    <!-- Audio Level Indicator -->
                    <ui:ProgressBar x:Name="AudioLevelBar" Value="0" Maximum="1" Height="3" 
                                  Width="240" Foreground="{StaticResource AccentBrush}"
                                  Background="{StaticResource SeparatorBrush}" 
                                  HorizontalAlignment="Center"/>
                </StackPanel>
                
                <!-- Results Header -->
                <Grid Grid.Row="1" Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBlock Grid.Column="0" Text="Transcription" FontSize="18" FontWeight="Medium" 
                             Foreground="{StaticResource TextPrimaryBrush}"/>
                    <TextBlock Grid.Column="1" x:Name="WordCountText" Text="0 words" FontSize="14" 
                             Foreground="{StaticResource TextTertiaryBrush}" VerticalAlignment="Center"/>
                </Grid>
                
                <!-- Results Text Area -->
                <Border Grid.Row="2" Background="{StaticResource CardSecondaryBrush}" 
                        CornerRadius="12" BorderThickness="0">
                    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                        <TextBox x:Name="ResultsTextBox" 
                               Background="Transparent" 
                               BorderThickness="0"
                               Foreground="{StaticResource TextPrimaryBrush}"
                               FontSize="15"
                               FontFamily="Segoe UI"
                               FontWeight="Normal"
                               TextWrapping="Wrap"
                               AcceptsReturn="True"
                               IsReadOnly="False"
                               Padding="24"
                               MinHeight="280"
                               VerticalAlignment="Stretch"/>
                    </ScrollViewer>
                </Border>
                
                <!-- Action Buttons -->
                <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                    <Button x:Name="CopyButton" Style="{StaticResource ModernButton}" 
                          Click="CopyButton_Click" IsEnabled="False" Margin="0,0,12,0">
                        <StackPanel Orientation="Horizontal">
                            <ui:FontIcon Glyph="&#xE8C8;" FontSize="14" Margin="0,0,8,0"/>
                            <TextBlock Text="Copy"/>
                        </StackPanel>
                    </Button>
                    
                    <Button x:Name="ClearButton" Style="{StaticResource SecondaryButton}" 
                          Click="ClearButton_Click" IsEnabled="False">
                        <StackPanel Orientation="Horizontal">
                            <ui:FontIcon Glyph="&#xE74D;" FontSize="14" Margin="0,0,8,0"/>
                            <TextBlock Text="Clear"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>
        
        <!-- Clean Footer -->
        <Grid Grid.Row="2" Margin="0,16,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <!-- Keyboard Shortcuts -->
            <StackPanel Grid.Column="0" Orientation="Horizontal">
                <Border Background="{StaticResource SeparatorBrush}" CornerRadius="6" Padding="8,4" Margin="0,0,8,0">
                    <TextBlock Text="Ctrl+Space" FontSize="11" Foreground="{StaticResource TextPrimaryBrush}" FontWeight="Medium"/>
                </Border>
                <TextBlock Text="Toggle Recording" FontSize="13" Foreground="{StaticResource TextTertiaryBrush}" VerticalAlignment="Center"/>
                
                <Rectangle Width="1" Height="14" Fill="{StaticResource SeparatorBrush}" Margin="16,0"/>
                
                <Border Background="{StaticResource SeparatorBrush}" CornerRadius="6" Padding="8,4" Margin="0,0,8,0">
                    <TextBlock Text="Ctrl+C" FontSize="11" Foreground="{StaticResource TextPrimaryBrush}" FontWeight="Medium"/>
                </Border>
                <TextBlock Text="Copy Text" FontSize="13" Foreground="{StaticResource TextTertiaryBrush}" VerticalAlignment="Center"/>
            </StackPanel>
            
            <!-- App Version -->
            <TextBlock Grid.Column="1" Text="v1.0.0" FontSize="12" Foreground="{StaticResource TextTertiaryBrush}" 
                     VerticalAlignment="Center"/>
        </Grid>
    </Grid>
</Window>